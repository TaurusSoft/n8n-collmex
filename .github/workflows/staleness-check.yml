name: Staleness Check

on:
  schedule:
    - cron: "0 6 * * 1"  # every Monday
  workflow_dispatch: {}

jobs:
  check:
    uses: TaurusSoft/.github/.github/workflows/n8n-staleness-check.yml@main
    permissions:
      contents: read
      issues: write
